server:
  port: 8585

spring:
  application:
    name: api-gateway

  cloud:
    gateway:
      routes:
        - id: auth-service
          uri: http://localhost:8484
          predicates:
            - Path=/api/auth/**

        - id: order-service
          uri: http://localhost:8686
          predicates:
            - Path=/api/orders/**
